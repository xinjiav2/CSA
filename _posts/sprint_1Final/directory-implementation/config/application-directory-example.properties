# Example only: create-drop destroys development data. Do not use on a shared database.
# Merge intentionally into a disposable backend profile; this file is not auto-loaded.
spring.datasource.url=jdbc:sqlite:/volumes/sqlite.db
spring.jpa.hibernate.ddl-auto=create-drop
